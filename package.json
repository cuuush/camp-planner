{
  "name": "camp-planner",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute camp-planner-db --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute camp-planner-db --remote --file=schema.sql",
    "db:seed:local": "wrangler d1 execute camp-planner-db --local --file=seed.sql",
    "db:seed:remote": "wrangler d1 execute camp-planner-db --remote --file=seed.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  }
}
